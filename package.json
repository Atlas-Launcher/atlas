{
  "name": "atlas-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "tauri:build": "turbo run tauri:build",
    "release:launcher": "turbo run release:launcher --filter=atlas --"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
